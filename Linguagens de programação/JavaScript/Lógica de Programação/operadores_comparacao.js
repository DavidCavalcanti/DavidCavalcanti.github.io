/**
 * Operadores de comparação
 * > maior que
 * >= maior ou igual a
 * > menor que
 * <= menor que ou igual a
 * == igualdade (checa apenas o valor) ->  não se utiliza
 * === igualdade estrita (checa o valor e tipo)
 * != diferente (checa valor) -> não se utiliza
 * !== diferente estrito (checa valor e tipo)
 */

const comparacao = 10 > 5;
console.log(comparacao);
